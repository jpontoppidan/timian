

function man() {
    this.x = width/2;
    
    this.show() = function(){ 
     man.show();
    man = new Man;
    
   
        fill(255);
        rect(this.x, height-20, 20, 20);
        }
}
}    
  this.move = function(dir)  {
      this.x += dir;
  }
